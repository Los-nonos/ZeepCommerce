{
  "name": "root",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-plugin-react": "^7.18.3",
    "lerna": "^3.20.2"
  },
  "scripts": {
    "delete-build": "sudo rm -rf packages/*/build packages/*/dist packages/website/static/build",
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "serve": "lerna run serve --stream",
    "test": "lerna run test",
    "linter:check": "lerna run linter:check",
    "prettier:check": "lerna run prettier:check",
    "prettier:run": "lerna run prettier:run",
    "commander:api": "docker exec -ti /zeepdocker_api_1 /bin/bash"
  },
  "workspaces": [
    "packages/*"
  ]
}
