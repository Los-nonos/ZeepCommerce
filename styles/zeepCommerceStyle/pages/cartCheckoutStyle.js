import { container, main, mainRaised } from '../../zeepCommerceStyles'

const styles = {
  container: {
    ...container,
    ...main,
    ...mainRaised,
    marginTop: '35vh'
  }
}

export default styles;